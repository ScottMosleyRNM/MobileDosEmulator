# Mobile DOS Emulator clean handoff

This bundle is the stable, buildable UI shell:
- sticky viewport
- load-game UI
- DOS-safe ZIP name normalization
- controller dock
- keyboard/F-key mode
- local snapshot UI

The emulator runtime is intentionally mocked in this handoff build so you can replace your repo contents and get back to a green GitHub Pages deploy immediately.
